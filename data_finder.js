/* finds group, period, electronic configuration, subshell electronic configuration,
   block and category from atomic number
*/

/**
 * get subshell electronic configuration from atomic numeber
 * @returns {string} subshell sequence
 */
function getSubElConfig (z) {
  // max e⁻ in every subshells
  var max = {s: 2, p: 6, d: 10, f: 14};
  // order of filling of electrons
  var order = [
    
  ]
}